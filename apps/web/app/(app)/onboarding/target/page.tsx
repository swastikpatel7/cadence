import { TargetStep } from '@/components/onboarding/steps/target-step';

export const metadata = {
  title: 'Going for a number? — Cadence',
};

export default function OnboardingTargetPage() {
  return <TargetStep />;
}
