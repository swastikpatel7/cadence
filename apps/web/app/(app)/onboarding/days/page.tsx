import { DaysStep } from '@/components/onboarding/steps/days-step';

export const metadata = {
  title: 'How often per week? — Cadence',
};

export default function OnboardingDaysPage() {
  return <DaysStep />;
}
