import { FocusStep } from '@/components/onboarding/steps/focus-step';

export const metadata = {
  title: 'What\'s your move? — Cadence',
};

export default function OnboardingFocusPage() {
  return <FocusStep />;
}
