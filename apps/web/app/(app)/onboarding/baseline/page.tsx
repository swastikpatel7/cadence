import { BaselineStep } from '@/components/onboarding/steps/baseline-step';

export const metadata = {
  title: 'Reading your runs… — Cadence',
};

export default function OnboardingBaselinePage() {
  return <BaselineStep />;
}
